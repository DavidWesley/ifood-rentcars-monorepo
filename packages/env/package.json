{
  "name": "@repo/env",
  "version": "1.0.1",
  "private": true,
  "description": "Pacote que controla as variáveis de ambiente da aplicação",
  "main": "./src/main.ts",
  "types": "./src/main.ts",
  "type": "module",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup src --format 'esm' --target 'esnext' --keep-names --clean --minify-syntax"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.28",
    "dotenv": "^16.4.5",
    "tsup": "^8.0.2"
  }
}
